# Fix: 'react-scripts' is not recognized

## Steps
- [x] Diagnose corrupted node_modules (approved plan)
- [ ] Delete corrupted `node_modules` directory
- [ ] Delete stale `package-lock.json`
- [ ] Run fresh `npm install` in `frontend/Mj-app/`
- [ ] Verify `node_modules/.bin/react-scripts` exists
- [ ] Test `npm start` works

